#include "main.h"
#include <stdio.h>

/**
 *add - function that adds two integers and returns the result
 *@a: integer 1
 *@b: integer 2
 * Return: 0 success
 */
int add(int a, int b)
{
	return (a + b);
}
